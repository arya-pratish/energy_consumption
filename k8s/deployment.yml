apiVersion: apps/v1
kind: Deployment
metadata:
  name: energy-prediction
spec:
  replicas: 3
  selector:
    matchLabels:
      app: energy-pred
  template:
    metadata:
      labels:
        app: energy-pred
    spec:
      containers:
      - name: energy-pred
        image: pratisharya/energy_cons_pred:latest
        ports:
        - containerPort: 80
        readinessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 10

---

apiVersion: v1
kind: Service
metadata:
  name: energy-pred-service
spec:
  type: LoadBalancer
  selector:
    app: energy-pred
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000